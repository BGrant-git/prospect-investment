import { Container, HeroWrapper, InfoWrapper } from './developmentsStyles'

const Developments = (props) => {
	return (
		<Container>
			<HeroWrapper></HeroWrapper>
			<InfoWrapper>dev comp</InfoWrapper>
		</Container>
	)
}

export default Developments
