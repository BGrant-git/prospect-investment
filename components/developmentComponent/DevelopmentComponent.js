import {
	Container,
	HeroWrapper,
	InfoWrapper,
} from './developmentComponentStyles'

const DevelopmentComponent = (props) => {
	return (
		<Container>
			<HeroWrapper></HeroWrapper>
			<InfoWrapper>dev comp</InfoWrapper>
		</Container>
	)
}

export default DevelopmentComponent
